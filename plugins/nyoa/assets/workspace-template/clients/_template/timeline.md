# Timeline — {{client_name}}

Append-only log of every interaction. Newest entries at the bottom.

Format:

```
## YYYY-MM-DD HH:MM — <channel: call/text/email/in-person/other>
<one paragraph: what happened, what was decided, what's next>
Next step: <action + owner + when>
```
